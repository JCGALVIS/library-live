/// <reference types="react" />
import { MediaPlayer } from "../../typings/MediaPlayer";
declare const StreamPlayer: ({ player, streamUrl, }: {
    player: MediaPlayer;
    streamUrl: string;
}) => JSX.Element;
export default StreamPlayer;
